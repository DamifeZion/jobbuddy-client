import { Card, CardHeader, CardTitle } from "../ui/card";

export const PersonalInformation = () => {
   return (
      <Card>
         <CardHeader>
            <CardTitle>Personal Information</CardTitle>
         </CardHeader>

         <div className="p-[inherit]"></div>
      </Card>
   );
};
