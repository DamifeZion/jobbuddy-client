import { cn } from "@/lib/utils";
import type { Metadata } from "next";
import { Roboto } from "next/font/google";
import ReduxProvider from "@/services/redux-provider/redux-provider";
import ToastProvider from "@/services/ToastProvider";
import "./css/globals.css";
import { TooltipProvider } from "@/components/ui/tooltip";

const roboto = Roboto({
   weight: "400",
   subsets: ["latin"],
   variable: "--font-roboto",
});

export const metadata: Metadata = {
   title: "Job Buddy",
   description: "Generated by create next app",
};

export default function RootLayout({
   children,
}: Readonly<{ children: React.ReactNode }>) {
   return (
      <html lang="en" suppressHydrationWarning>
         <body
            className={cn(roboto.className, {
               // "debug-screens": process.env.NODE_ENV === "development",
            })}
         >
            <ReduxProvider>
               <ToastProvider />
               {/*NOTE: We use tooltip provider here globally, to ensure no need for re-use other places */}
               <TooltipProvider delayDuration={50}>{children}</TooltipProvider>
            </ReduxProvider>
         </body>
      </html>
   );
}
