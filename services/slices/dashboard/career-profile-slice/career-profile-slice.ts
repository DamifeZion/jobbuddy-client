import { CareerSliceProps } from "@/types";
import { createSlice } from "@reduxjs/toolkit";

const initialState: CareerSliceProps = {};

export const careerSlice = createSlice({
   name: "Career Slice",

   initialState,

   reducers: {},
});

export const {} = careerSlice.actions;
